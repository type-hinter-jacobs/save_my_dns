[project]
name = "save-my-dns"
version = "0.1.0"
description = "Local DNS filtering proof-of-concept"
requires-python = ">=3.10"

dependencies = [
    "dnslib",
    "sqlalchemy>=2.0",
    "pytest",
    "fastapi",
    "uvicorn[standard]",
    "httpx",
    "jinja2",
    "python-multipart",
    "itsdangerous"
]